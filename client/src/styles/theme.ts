import { Theme } from '@emotion/react';

const mainColor: string = '#BABBBD';
const subColor: string = '#868387';
const thirdColor: string = '#676168';
const theme: Theme = {
  colors: {
    mainColor,
    subColor,
    thirdColor,
  },
};

export default theme;
